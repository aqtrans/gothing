{{ define "title" }}<title>{{ .page.Title }} - {{.page.TheName}}</title>{{ end }}
{{ define "sidebar" }}
    {{ template "sidebar_nav" . }}
{{ end }}
{{ define "content" }}     
<h1>{{ .page.Title }}</h1>
{{$title := .page.Title}}
<hr />
<div>
{{.MD}}
</div>
{{ end }}
